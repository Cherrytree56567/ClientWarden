<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WindowsUI.PasswordEditField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button Margin="0, 8, 0, 0"
            Padding="0"
            Background="Transparent"
            CornerRadius="6"
            BorderThickness="0"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Stretch">
        <Border Background="{ThemeResource ControlFillColorDefaultBrush}"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10, 8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="2">
                    <TextBlock Text="Password" 
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               x:Name="FieldName"/>
                    <ListView x:Name="PasswordGenMode" 
                              SelectionMode="Single" 
                              Margin="-8, 0, 0, 0" 
                              SelectedIndex="0"
                              SelectionChanged="PassGenList_Changed">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        
                        <ListViewItem Content="Random"/>
                        <ListViewItem Content="Pin"/>
                        <ListViewItem Content="Memorable"/>
                    </ListView>
                    <StackPanel Spacing="8" Orientation="Horizontal" x:Name="PassGenOptions">
                        <CheckBox x:Name="PassGenNum" Content="Numbers" IsChecked="True"/>
                        <CheckBox x:Name="PassGenSym" Content="Symbols" IsChecked="True"/>
                        <CheckBox x:Name="PassGenCap" Content="Caps" IsChecked="True"/>
                    </StackPanel>
                    <Slider x:Name="PassGenSlider"
                            Minimum="1" 
                            Maximum="64"
                            Value="14"/>
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox PlaceholderText="Password123"
                                     FontSize="14"
                                     x:Name="FieldValue"
                                     Margin="-10, 2, 0, -8"
                                     CornerRadius="0,0,4,4"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     PasswordRevealMode="Peek"
                                     Grid.Column="0"/>
                        <Image Grid.Column="1"
                               Height="16"
                               Width="16"
                               Source="ms-appx:///Assets/ic_fluent_arrow_clockwise_dashes_24_regular.png"
                               VerticalAlignment="Center"
                               Margin="8, 0, 0, -8"
                               x:Name="PassGenBtn"
                               Tapped="PassGen_Click"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Button>
</UserControl>
